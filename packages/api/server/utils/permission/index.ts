import can from './can';
import getPermission from './getPermission';
import restrict from './restrict';
import where from './where';

export default {
    can,
    getPermission,
    where,
    restrict,
};
