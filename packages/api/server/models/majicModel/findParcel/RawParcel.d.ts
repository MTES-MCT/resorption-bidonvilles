export type RawParcel = {
    idpar: string, // Identifiant dfe la parcelle
    idcom: string, // Code de la commune
    dnupro: string, // Numéro de propriété
    dnuvoi: string, // Numéro de voirie
    cconvo: string, // Code de la nature de voie
    dvoilib: string, // Libellé de la voie
    idcomtxt: string, // Libellé de commune complet (Avec accents, source INSEE)
};
