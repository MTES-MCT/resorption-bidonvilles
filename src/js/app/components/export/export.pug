<div class="alert">
    <div class="alert-wrapper" ref="wrapper">
        <header class="alert-header">
            <hgroup>
                <h1 class="alert-title">Exporter les sites {{ title }} :</h1>
                <h2 class="alert-subtitle">{{ location.label }}</h2>
            </hgroup>

            <div class="alert-actionButtons">
                <a href="#" ref="link" style="display: none"></a>
                <button @click="close" class="button secondary">Annuler</button>
                <button @click="download" class="button"><font-awesome-icon icon="file-excel"></font-awesome-icon> &nbsp;Exporter</button>
            </div>
        </header>

        <section class="alert-body">
            <p>
                <strong>Les données exportées par défaut</strong>
                <ul class="list--circle">
                    <li>Localisation : département, commune, adresse</li>
                    <li>Site : type de site (terrain / bâti), date d’installation, date de signalement</li>
                    <li>Habitants : nombre de personnes, mineurs, ménages et origines</li>
                    <li v-if="closedTowns">Orientations des ménages après la fermeture</li>
                    <li>Date de la dernière mise à jour</li>
                </ul>
            </p>

            <p class="alert-options">Cochez les informations supplémentaires que vous souhaitez exporter
                <ul>
                    <li v-for="option in availableOptions">
                        <span :class="`option-${option.id}`">
                            <input type="checkbox" :id="`option-${option.id}`" v-model="options[option.id]" />
                            <label :for="`option-${option.id}`" >{{ option.label }}<span v-if="option.description">&nbsp;{{ option.description }}</span></label>
                        </span>
                    </li>
                </ul>
            </p>
        </section>
    </div>
</div>
